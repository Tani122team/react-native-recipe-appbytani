# Copy this file to .env and set your Railway public HTTPS URL (from Railway → service → Settings → Networking / Generate domain).
# Do not commit .env. /api is added automatically if you omit it.
EXPO_PUBLIC_API_URL=https://your-service.up.railway.app
